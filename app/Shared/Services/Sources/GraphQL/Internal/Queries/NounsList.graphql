query NounsList($skip: Int, $first: Int) {
  nouns(skip: $skip, first: $first) {
    id
    seed {
      background
      body
      accessory
      head
      glasses
    }
    owner {
      id
    }
  }
}
